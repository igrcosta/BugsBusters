using System;
using UnityEngine;

/*public class DeathOnDamage : MonoBehaviour, IDamageable
{   //essa classe destrói o objeto que a possui ao receber dano
    public event Action DamageEvent;

    public void TakeDamage(int damage)
    {
        DamageEvent.Invoke();
    }
}
*/